# 面向工业光伏与农业作业的多目标无人机路径规划研究（论文提纲）

<!-- > 本文档与仓库实现对应：**低层**为 Microsoft AirSim + Gym 环境下的连续控制避障策略（Stable-Baselines3 **SAC**，见 `scripts/utils/thread_train*.py` 与评估线程）；**高层**为 TensorFlow 1.x **指针网络**强化学习求解 TSP（见 `PointerNetwork-RL-TSP_new/PointerNetwork/`，含 `classic_tsp` 与 `tsp_with_obstacles`）；**串联评测**见 `scripts/dataset_test.py`（批量读取 `coord_sets_no_obstacles.npy` / `coord_sets_with_obstacles.npy` 等有序航点序列并调用 `EvaluateThread`）。 -->

---

## 摘要

面向**工业场景**光伏板的污染与损坏识别、清洗作业，以及**农业场景**地块识别后的农药精准喷洒等典型无人机任务，本文研究的前提是：**无人机已通过全域扫描获得目标位置**（如光伏板块代表点、地块代表点），研究重点转为后续任务执行阶段中，无人机如何规划**访问各目标点的次序**，并在相邻目标之间完成**障碍环境下的安全飞行**。

为此，本文采用**分层串联**路径规划框架：（1）在 AirSim 仿真中，基于深度强化学习 **SAC（Soft Actor-Critic）** 训练点到点的局部避障导航策略，作为可复用的低层执行模块；（2）将多目标访问抽象为**旅行商问题（TSP）**，采用**基于策略梯度的指针网络（Pointer Network）**学习较短的全局访问序列；（3）将两层衔接——先由 TSP 模型输出有序坐标序列，再在相邻点对之间**调用 SAC 避障策略**完成子段导航，形成“全局次序 + 局部避障”的完整链路。

针对低层强化学习在**贴近障碍物时因策略随机性（探索）易触碰障碍导致子段失败**的问题，（4）在指针网络训练中引入**带障碍的任务地图先验**：在奖励中叠加路径长度与**线段–障碍相交（硬惩罚）、近障（软惩罚）**等项，使学得序列所诱导的点对之间直线几何关系**尽可能减少穿障与擦边**，从而在可接受范围内**以略长的回路长度换取更高的后续避障成功率**。工程上体现“**安全（成功率）优先于名义最短路径**”的权衡。

实验基于本项目脚本生成多组目标序列并在 AirSim 中批量评估。结果表明，该分层方案模块边界清晰、便于复现与扩展，可为固定或半固定场景下的多目标无人机作业提供一种可行技术路径。未来可进一步研究仿真–实景地图对齐、vln端到端路径规划。

**关键词**：工业无人机；农业无人机；光伏巡检与清洗；多目标路径规划；旅行商问题（TSP）；指针网络；深度强化学习；SAC；AirSim；分层规划；障碍感知序列规划

---

## 目录与各章概要

### 第1章 绪论

| 小节                       | 概要内容                                                                                                                                                             |
| -------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **1.1 研究背景与意义**     | 工业：光伏板污染/损坏巡检与清洗对多航点、高效率与安全飞行的需求；农业：地块识别后的分区施药与航迹组织；无人机在已知目标点集合上批量作业的普遍性。                    |
| **1.2 研究问题与挑战**     | 在**全局扫描已完成、目标位置已知**前提下，聚焦“访问次序 + 连续避障执行”；离散组合优化（TSP）与连续控制（RL）的接口与误差传递；近障时 RL 探索导致的失效与成功率约束。 |
| **1.3 研究内容与技术路线** | 四段式路线：AirSim–SAC 点到点避障 → 指针网络 RL 求 TSP 序列 → 序列驱动下逐段调用 SAC → 障碍地图融入 TSP 训练以提升串联安全性。                                       |
| **1.4 主要贡献与创新点**   | 分层架构的工程化落地；障碍感知 TSP 训练以缓解底层 RL 近障弱点；面向光伏与农业两类任务的统一“已知目标点后的路径规划”表述。                                            |
| **1.5 论文结构安排**       |                                                                                                                                                                      |

### 第2章 相关工作

| 小节                                                 | 概要内容                                                                   |
| ---------------------------------------------------- | -------------------------------------------------------------------------- |
| **2.1 无人机路径规划与多航点作业**                   | 巡检、植保、清洗等多目标访问；本文定位于“目标点已给出后的次序与局部导航”。 |
| **2.2 神经组合优化与指针网络求解 TSP**               | Pointer Network、策略梯度训练 TSP；与精确算法/启发式的精度–效率权衡。      |
| **2.3 深度强化学习在无人机避障与点到点飞行中的应用** | 仿真平台（含 AirSim）、连续控制算法（SAC 等）与 SB3 工程栈。               |
| **2.4 分层规划（高层离散 + 低层连续）**              | 任务分解、接口设计；本文“先序列后点对点策略”范式。                         |
| **2.5 本文方法定位与差异**                           | 与纯几何规划、端到端策略的区别；高层注入障碍代价以适配低层 RL 的动机。     |

### 第3章 问题定义与系统总体设计

| 小节                       | 概要内容                                                                                                  |
| -------------------------- | --------------------------------------------------------------------------------------------------------- |
| **3.1 任务定义**           | 已知目标点集合与障碍区域；优化目标为可完成的访问次序及可接受的飞行代价，**安全优先**。                    |
| **3.2 系统总体架构**       | **高层**：指针网络输出访问序列；**低层**：SAC 执行相邻目标间导航；数据流与模块调用关系。                  |
| **3.3 坐标系、尺度与假设** | 工作区范围、统一飞行高度、仿真与 TSP 输入坐标对齐假设。                                                   |
| **3.4 评价指标**           | TSP 回路长度、穿障/近障惩罚统计、AirSim 串联 **episode 成功率**、成功条件下的总航程与 `min_distance` 等。 |

### 第4章 低层导航：基于 SAC 的 AirSim 点到点避障模型（第一部分）

| 小节                                          | 概要内容                                                                          |
| --------------------------------------------- | --------------------------------------------------------------------------------- |
| **4.1 仿真环境与 Gym 接口**                   | `gym_airsim_multirotor` 环境；观测、动作空间与奖励设计概要。                      |
| **4.2 SAC 算法与 Stable-Baselines3 训练流程** | 连续动作、最大熵 RL 的特点；`thread_train.py` 缓冲区、噪声与超参数。              |
| **4.3 点到点导航任务建模**                    | 子目标到达判定、终止条件、碰撞判定；策略作为高层给出的下一航点的**局部执行器**。  |
| **4.4 模型保存与评估接口**                    | `model_sb3.zip` 加载；`EvaluateThread` / `dataset_test.py` 多组航点序列评测流程。 |

### 第5章 高层规划：基于指针网络强化学习的 TSP 访问序列（第二部分）

| 小节                                                       | 概要内容                                                                                                                                                                |
| ---------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **5.1 从多目标作业到 TSP 的抽象**                          | 目标点集合、闭合回路；光伏板块中心点、地块代表点等与 TSP 节点的对应关系。                                                                                               |
| **5.2 指针网络结构与训练**                                 | `actor.py`、`decoder.py`、`critic.py`：编码–解码指针机制与策略梯度优化。                                                                                                |
| **5.3 经典 TSP 基准（`classic_tsp`）**                     | 以回路长度为主目标，`./save/` 与 `goal_sequence.npy`；作为带障碍训练的对照基线。                                                                                        |
| **5.4 障碍感知 TSP（第四部分核心：`tsp_with_obstacles`）** | `dataset.py` 中方形/圆形障碍与安全余量；`actor.py` 中路径长度项与障碍惩罚、`feasible_tour_bonus`；`./save_obsta/` 权重与序列导出；**允许路径变长以换取更少穿障/近障**。 |

### 第6章 两层模型串联与任务执行流程（第三部分）

| 小节                            | 概要内容                                                                                                       |
| ------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| **6.1 串联逻辑**                | 输入目标点集 → TSP 模型输出有序序列 → 对相邻点对依次调用 SAC 直至遍历完成。                                    |
| **6.2 航点切换与 Episode 组织** | 多航点 episode 内子目标切换。                                                                                  |
| **6.3 失败模式分析**            | 近障探索触碰障碍导致子段失败；说明第四部分通过高层序列几何改善低层成功率的机理。                               |
| **6.4 与代码模块的对应关系**    | `test_100_sequence.py` / `coord_sets_*.npy` 生成；`dataset_test.py` 批量对比无障碍序列与带障碍序列的评估结果。 |

### 第7章 实验设计与结果分析

| 小节                     | 概要内容                                                                                                             |
| ------------------------ | -------------------------------------------------------------------------------------------------------------------- |
| **7.1 实验平台与软件栈** | Windows、AirSim、Python；SB3（PyTorch）与 TF1.x 指针网络并存说明。                                                   |
| **7.2 数据集与对照设置** | 多组随机/固定实例；`classic_tsp` vs `tsp_with_obstacles`；`coord_sets_no_obstacles` vs `coord_sets_with_obstacles`。 |
| **7.3 高层规划结果**     | 回路长度、障碍惩罚指标；不同 `penalty_scale`、`path_length_weight` 的敏感性。                                        |
| **7.4 串联仿真结果**     | `dataset_test.py` 统计的成功率、路程；验证“略增全局路程、提升成功率”假设。                                           |
| **7.5 消融与讨论**       | 去掉障碍惩罚 / 惩罚 、地图误差与局限。                                                                               |

### 第8章 总结与展望

| 小节             | 概要内容                                                       |
| ---------------- | -------------------------------------------------------------- |
| **8.1 工作总结** | 归纳四部分工作与工业光伏、农业植保两类场景的对应关系。         |
| **8.2 未来工作** | 传感器与地图严格对齐、动态障碍、实机部署、联合或分层协同优化。 |

---

<!--
### 附录与参考文献（可选列入正式目录）

- 符号表、算法伪代码、关键超参数表（SAC `config.ini` 与 `PointerNetwork` `config.py`）。
- 复现实验步骤：训练命令（`main.py --mode classic_tsp` / `tsp_with_obstacles`）、评测命令（`dataset_test.py`）、路径占位符说明。
- 参考文献（按学校模板 GB/T 7714 等格式整理）。 -->
